文字批处理分为两个方面：

一、顶点位置批处理

1）跟随地图的文字，每个label 有旋转角度，不同位置。
	位置可以批处理，关键是 “旋转角度” 不同导致变换不同 【木办法批处理】
	
2）HUD显示的字体，始终垂直于视线方向。
	统一的变换，不同位置通过CPU预先计算 【可以批处理】

二、纹理批处理

TextureAtlas 
	256*256 能放16*16汉字符 256个。

需要考虑不同文字、不同文字效果

